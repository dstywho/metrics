<h1>Listing grading_scales</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @grading_scales.each do |grading_scale| %>
  <tr>
    <td><%= grading_scale.name %></td>
    <td><%= link_to 'Show', grading_scale %></td>
    <td><%= link_to 'Edit', edit_grading_scale_path(grading_scale) %></td>
    <td><%= link_to 'Destroy', grading_scale, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Grading scale', new_grading_scale_path %>
